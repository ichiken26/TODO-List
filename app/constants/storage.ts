/**
 * ストレージ（localStorage）に関する定数定義
 */

/**
 * localStorageのキー
 * アプリケーションで使用するlocalStorageのキーを定義
 */
export const STORAGE_KEYS = {
  /**
   * チェック画面のチェック状態を保存するキー
   */
  CHECKED_STATE: 'todo-checked-state',
} as const;

